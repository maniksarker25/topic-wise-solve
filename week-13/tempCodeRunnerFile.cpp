 // ll last_value = maxVal - x;
        // for (int i = n - 2; i >= 0; --i) {
        //     if (a[i] != last_value) {
        //         break;
        //     }
        //     last_value -= x;
        //     ++operation;
        // }